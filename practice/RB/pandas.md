1.DataFrame:
2.Index:
    Able To Create Index based on SetIndex() commands and able to acces loc function with input string,
3.Access DataFrame:
    i.loc
        [{Index_Value},{Only_One_Column_Selection}]
    loc
        [{Index_Value},{Multiple_Column_Selection}]
    at
        Accessing Position Based on Label[ColumnName]
    iat
        Accessing Position based on [Row and Column]